{
  "name": "lia-annotation",
  "version": "1.0.2",
  "description": "LiaScript annotation overlay plugin — pen, eraser, undo/redo toolbar for live presentations",
  "scripts": {
    "build": "parcel build src/index.ts --dist-dir dist --no-scope-hoist"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "typescript": "^5.8.3"
  }
}
